[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd ../.. && npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npx prisma generate", "npx nest build"]

[start]
cmd = "node dist/main.js"
